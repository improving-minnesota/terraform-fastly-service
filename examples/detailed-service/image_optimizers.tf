locals {
  image_optimizers = [
    # Disabling, as this is requires product enablement
    # {
    #   jpeg_type     = "auto"
    #   name          = "Sample Image Optimizer"
    #   resize_filter = "lanczos2"
    # },
  ]
}
